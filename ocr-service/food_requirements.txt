# Food Classification Service Requirements

# Core ML Framework
torch>=2.2.0
numpy>=2.0.0

# Vision & NLP Models
transformers>=4.45.0
huggingface-hub>=0.20.0

# Image Processing
Pillow>=10.0.0
opencv-python>=4.8.0
opencv-contrib-python>=4.8.0

# Web Framework
fastapi>=0.110.0
uvicorn>=0.27.0
python-multipart>=0.0.7

# Utilities
requests>=2.31.0
python-dotenv>=1.0.0

# Calorie Calculation
scipy>=1.11.0
pandas>=2.0.0

# Optional accelerators
timm>=0.9.7

# System Requirements:
# 1. Install PyTorch with CPU support (already included above)
# 2. For GPU support, install matching CUDA build of torch + torchvision
# 3. Memory usage: ~1-2GB RAM
# 4. Model download: ~500MB initial download

# Expected Performance:
# - Accuracy: 85-95% on food classification
# - Processing time: 1-3 seconds per image
# - Supported formats: JPEG, PNG, WebP
# - Max image size: 10MB
